<Window x:Class="Contacts.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:Contacts"
        mc:Ignorable="d"
        Icon="contacts_96x96.ico"
        Title="Contacts" Height="375" Width="500" MinWidth="500" MinHeight="300" Padding="15">
        
    <Grid Margin="15,15,15,15">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="1*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Edit Contact Section -->
        <GroupBox Header="Edit Contact" Grid.Column="0" Margin="0,0,3,0">
            <Grid Margin="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Name:" Grid.Row="0"/>
                <TextBox Grid.Row="1" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Phone Number:" Grid.Row="2"/>
                <TextBox Grid.Row="3" Margin="0,0,0,5" HorizontalAlignment="Stretch"/>

                <TextBlock Text="Email:" Grid.Row="4"/>
                <TextBox Grid.Row="5" Margin="0,0,0,5" VerticalAlignment="Top" HorizontalAlignment="Stretch" Height="18"/>
            </Grid>
        </GroupBox>

        <!-- Read Contact Section -->
        <GroupBox Header="Read Contact" Grid.Column="1" Margin="3,0,0,0">
            <Grid Margin="3">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Text="Name:"  Grid.Row="0"/>
                <TextBlock Text="Смирнов Юрий" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="1"/>

                <TextBlock Text="Phone Number:" Grid.Row="2"/>
                <TextBlock Text="+7-913-111-22-33"  FontWeight="Bold" Margin="0,0,0,5" Grid.Row="3"/>

                <TextBlock Text="Email:" Grid.Row="4"/>
                <TextBlock Text="yuri.smirnov@no.mail" FontWeight="Bold" Margin="0,0,0,5" Grid.Row="5"/>
            </Grid>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.Row="1" Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Right" VerticalAlignment="Bottom" Margin="0,10,0,0">
            <Button Content="Save" Width="75" Height="25" Margin="0,0,5,0"/>
            <Button Content="Load" Width="75" Height="25"/>
        </StackPanel>
    </Grid>
</Window>
